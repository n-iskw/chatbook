# セキュリティについて

## 脆弱性の報告

**公開の Issue には書かないでください。** GitHub の
[Security Advisory](https://github.com/skanehira/chatbook/security/advisories/new) から
非公開で報告してください。作者 1 人で趣味として開発しているため、応答までに数日かかります。

報告に含めてほしいもの: 再現手順、影響（何が読めてしまう / 書けてしまうか）、確認した
バージョン（コミットハッシュ）。

## このアプリの前提

セルフホスト前提なので、**動いているインスタンスの安全はデプロイした人の責任**です。
設計上の前提を知っておいてください。

- **利用者は 1 人。** ログインした人が全データの持ち主で、D1 に所有者の列はありません。
  複数人で共有すると、全員が互いの本・ハイライト・チャット履歴を読めます
- **認証は ID とパスワード 1 組だけ。** `AUTH_USERNAME` / `AUTH_PASSWORD` を
  `wrangler secret put` で設定します。**総当たりに対する制限はありません**ので、
  長いパスワードを使ってください
- **セッションは HMAC で署名した Cookie 1 本**（30 日、`Secure` 付き）。サーバに記録を
  持たないため個別に失効させられません。端末を失くしたときの取り消し手段は
  `AUTH_SESSION_SECRET` の入れ替えだけで、入れ替えると全端末がログアウトします
- **秘密が設定されていなければ API は全部 401 で閉じます。** 設定を忘れたまま公開して
  しまう事故を防ぐためで、初回のデプロイは意図的に閉じた状態で出ます
- **選択した本文は DeepSeek の API へ送られます。** 本の全文をコンテキストに載せるため、
  読ませたくない文書は開かないでください

## サポート対象

`main` の最新コミットのみです。過去のコミットに対する修正は出しません。
